import React from 'react';
import { Table } from 'reactstrap';

const TableBody = (props) => {
    return (
        <tbody>
            {/* users.map(
            return ( */}
            <tr> props go here </tr>
          )
         )
        </tbody>
    );
}

export default TableBody;