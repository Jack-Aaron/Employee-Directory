import React from 'react';

const TableBody = (props) => {
    return (
        <tbody>
            {/* users.map(
            return ( */}
            <tr> props go here </tr>
          )
         )
        </tbody>
    );
}

export default TableBody;